# Ell-ena AI Assistant Demo

This repository contains the proof-of-concept demo video for the Ell-ena AI assistant prototype built for the GSoC proposal.

The demo shows a Streamlit interface querying project tasks using a self-hosted LLM (Mistral) via Ollama and a FastAPI backend.
